import {createBrowserHistory} from 'history';

/**
 * @description Overrides the default implementation of BrowserHistory and 
 * passed as prop to the Router component defined in App
 */
export default createBrowserHistory();